#!/bin/bash
# hello_world.sh - Simple example script for voice commands

# Get current time
CURRENT_TIME=$(date +"%H:%M:%S")

# Output message (this will appear on the LCD display)
echo "Hello! $CURRENT_TIME"

# Exit successfully
exit 0
